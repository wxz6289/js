// 默认参数形成独立参数作用域；y = x 引用同作用域内的 x

var x = 1;

function f(a, y = x) {
  console.log('y =', y);
}

f(2); // y = 1 — 参数作用域内的 x 取外层 var x，而非形参 a

// 一旦设置了默认参数，函数声明初始化时参数会形成单独的作用域，
// 初始化结束后该作用域消失。未设置默认参数时不会出现此行为。

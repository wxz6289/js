在文件或函数体`第一行`使用`"use strict"`指令来指示浏览器以严格模式运行,并且无法取消。
1. "use strict" 指令将浏览器引擎转换为“现代”模式，改变一些内建特性的行为。
2. 严格模式通过顶部的 "use strict" 启用。一些新语言特性诸如 “classes” 和 “modules” 也会自动开启严格模式。
3. 所有的现代浏览器都支持严格模式。
4. 建议始终使用 "use strict" 启动脚本。

严格模式下，给未声明的变量赋值会报错。